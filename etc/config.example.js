(function (main) {
    main.config = {

        // List of any other scripts to load
        //
        // paths are relative to the index.html file
        plugins: [
            "../path/to/script.js",
            "../path/to/another/script.js",
        ]
    }
})(main)